[package]
name = "codepack-tokens"
description = "Byte and token accounting, and context-budget selection."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
serde.workspace = true
# Runtime, not dev-only: `ModelContextLimits::load_or_default` reads a user-supplied
# override file so model limits can be refreshed without a rebuild (BLUEPRINT §B.2).
serde_json.workspace = true
thiserror.workspace = true

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
