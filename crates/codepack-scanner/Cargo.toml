[package]
name = "codepack-scanner"
description = "Tree walking, ignore rules, stack detection, and export planning."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
codepack-core.workspace = true
codepack-tokens.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
walkdir.workspace = true
rayon.workspace = true

[dev-dependencies]
tempfile.workspace = true
# Test-only. `.exportignore` globs are matched directly rather than translated into
# regex source and compiled; `regex` stays as the reference the matcher is differentially
# checked against, including a verbatim copy of the translation it replaced.
regex.workspace = true

[lints]
workspace = true
