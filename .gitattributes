# Enforce LF line endings for all text files to match ruff line-ending = "lf"
* text=auto eol=lf

# Explicitly keep CRLF only for Windows-specific scripts
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
