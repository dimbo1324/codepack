"""Tests for the shared toolkit — the infrastructure every script depends on.

A defect here is a defect in all eight scripts at once, which is why the toolkit carries
its own tests rather than being covered only through whichever script happens to use it.
"""
