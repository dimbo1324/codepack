{
  "_comment": "Verifies the developer scripts themselves. stdlib unittest only -- adding pytest as a dependency just to test a handful of helpers would be heavier than the thing being tested.",
  "steps": [
    {
      "label": "unit tests under scripts/ (stdlib unittest)",
      "argv": ["python", "-m", "unittest", "discover", "-s", "scripts", "-t", "."],
      "required": true
    },
    {
      "label": "orchestrator catalog loads and validates",
      "argv": ["python", "dev_tools_scripts_runner.py", "list"],
      "required": true
    }
  ]
}
