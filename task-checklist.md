# Task Checklist

**Status:** в работе.

**Task:** Аудит и модернизация ядра без добавления новых возможностей: устранение
дублирования, замена регулярных выражений строковыми операциями там, где это уместно,
именование магических чисел, стандартизация комментариев, расширение тестов.
**Date:** 2026-07-25
**Branch:** refactor/core-audit-dedup-and-regex-removal

Задача поставлена владельцем как ревизия уже сданного транша «Ядро» (S0–S9), а не как
новый этап `ROADMAP.md`. Новая бизнес-логика не добавляется: всё, что меняется, обязано
сохранить наблюдаемое поведение.

## Preparation

- [ ] Ритуал ориентации (git status/log, ROADMAP, overview.md, task-checklist.md,
      open-questions.md)
- [ ] Зафиксировать базовые показатели ДО правок, чтобы сравнивать не по памяти:
      662 проходящих теста, golden 3/3, корпус-тест зелёный (инвариант I9)
- [ ] Рабочая ветка создана, чек-лист закоммичен до основной работы

## Аудит — что найдено разведкой

- [ ] Алгоритм гражданской даты (`civil_from_days`/`format_unix_seconds`) продублирован
      в 8 файлах; три копии байт-в-байт идентичны
- [ ] `format_bytes` существует в двух крейтах (`-tokens` — канонический,
      `-archive` — копия)
- [ ] Логика «разрезать по первому `=`/`:`, санитизировать префикс, отформатировать»
      продублирована в `redact.rs::replace_match` и `keyword.rs::redacted_line`
- [ ] Набор маркеров TODO/FIXME задан трижды; в `refactoring.rs` у него реальный дефект
      приоритета операций (`\b` связывается только с крайними альтернативами)
- [ ] Строка `.expect("hand-written pattern literal is a valid regex…")` повторена 19 раз
- [ ] Магические числа: ширины колонок и разделителей отчётов (100/120/20/12), пороги

## Фаза 1 — дедупликация примитивов

- [ ] `codepack-core::time` — единственная реализация гражданской даты и форматирования
      UTC; копии в `-archive`/`-scanner`/`-security` удалены, `-engine`/`-reports`
      переведены на неё
- [ ] `format_bytes` — `-archive` использует `codepack-tokens` (прецедент: `-scanner`
      уже так делает), собственная копия удалена
- [ ] Общий хелпер редакции `key=value` — одна реализация на оба пути (содержимое
      экспорта и сообщение находки)
- [ ] Единый набор маркеров TODO/FIXME; дефект приоритета в `refactoring.rs` исправлен,
      покрыт регрессионным тестом

## Фаза 2 — регулярные выражения → строковые операции

Граница решения: заменяются регулярки формы «литеральный префикс + набор символов +
ограничение длины» и поиск ключевых слов. Регулярки `codepack-reports`, разбирающие
синтаксис исходников с захватами, **сохраняются** — переписывание дало бы риск
регрессии без выигрыша.

- [ ] Примитив сканирования токенов (`codepack-security::patterns::token_scan`)
- [ ] 10 провайдер-сигнатур + telegram переведены на примитив
- [ ] 9 risky-code правил переведены на строковый поиск
- [ ] Keyword-каскад (`SECRET_PATTERNS`/`SECRET_KEY_PATTERN`/`ASSIGNMENT_SECRET_RE`/
      `PRIVATE_KEY_RE`) переведён на строковый поиск
- [ ] `codepack-security` больше не зависит от `regex`
- [ ] Рукописный glob-матчер в `codepack-scanner::ignore::pattern`
- [ ] `codepack-scanner` больше не зависит от `regex`
- [ ] Дифференциальные тесты «старое поведение против нового» написаны ДО удаления
      регулярок и оставлены как постоянное покрытие

## Фаза 3 — магические числа и комментарии

- [ ] Ширины и разделители отчётов вынесены в именованные константы с объяснением,
      почему именно такое значение (контракт формата legacy)
- [ ] Пороговые значения названы
- [ ] Комментарии приведены к единому стилю: объясняют «почему», а не пересказывают код

## Фаза 4 — тесты

- [ ] Каждый новый примитив покрыт модульными тестами, включая граничные случаи
- [ ] Регрессионные тесты на найденные дефекты
- [ ] Тесты не ослаблены и не удалены ради зелёного гейта

## Verification

- [ ] `cargo fmt --all --check`
- [ ] `cargo clippy --workspace --all-targets -- -D warnings`
- [ ] `cargo test --workspace` — не меньше базовых 662
- [ ] `cargo test -p codepack-engine --test golden` — 3/3, паритет не сломан
- [ ] Корпус реально перемерен: precision не упала (инвариант I9)
- [ ] `cargo xtask sync-agents --check`
- [ ] `cargo deny check` — недоступен в песочнице, проверяется в CI
- [ ] Независимое ревью диффа

## Completion

- [ ] `docs/architecture/overview.md` обновлён
- [ ] Чек-лист заполнен `+`/`-`
- [ ] Push ветки на `origin` (без слияния в `main` и без pull request — указание
      владельца)
- [ ] Финальный отчёт владельцу

## Next task

Этап **S10 — CLI / headless** (`ROADMAP.md` §3), там же Q6 (`.codepack.toml`).
