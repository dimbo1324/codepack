# Task Checklist

**Status:** реализация завершена, гейт зелёный.

**Task:** Закрытие Q16, Q7 и Q15 — решения владельца от 2026-07-25
(`docs/decisions/open-questions.md`), отдельной задачей до S10. Плюс запуск CI на
тематических ветках.
**Date:** 2026-07-25
**Branch:** feat/redaction-constants-aws-signature

## Preparation

- [+] Ритуал ориентации (git status/log, ROADMAP §1 и §8, overview.md, предыдущий
      task-checklist.md, open-questions.md)
- [+] Зафиксировать baseline корпуса ДО правок, чтобы сравнивать не по памяти:
      parity P=1.000 R=0.312 F1=0.476, full P=1.000 R=1.000 F1=1.000 (инвариант I9)
- [+] Зафиксировать, что golden 3/3 зелёный до правок

## CI на тематических ветках

- [+] `.github/workflows/ci.yml` — `branches: ['**']` в триггере `push`
- [+] Убедиться, что `concurrency`-группа по-прежнему отменяет устаревшие прогоны
      (иначе каждая ветка начнёт жечь минуты параллельно)

## Q16 — утечка I3, когда секрет содержит `=`

- [+] Воспроизвести утечку тестом ДО правки (`Authorization: Basic ...==`), чтобы было
      видно: тест ловит проблему, а не написан под готовый результат
- [+] `keyword::redacted_line`: считать текст до первого `=`/`:` именем ключа только
      если он похож на идентификатор; иначе редактировать строку целиком
- [+] Определить «похож на идентификатор» явно и обосновать в коде, а не подобрать
      порог под фикстуры
- [+] Golden проверен: **не разошёлся** (3/3), фиксировать отклонение не потребовалось
- [+] Корпус перемерить: precision не падает (I9)

## Q7 — константы классификации текст/бинарь в `codepack-core`

- [+] Перенести `TEXT_EXTENSIONS`/`BINARY_EXTENSIONS`/
      `TEXT_FILENAMES_WITHOUT_EXTENSION`/`should_consider_text_file`/`looks_binary`
      в `codepack-core`
- [+] `codepack-scanner` и `codepack-security` используют перенесённое, дубль удалён
- [+] Публичная поверхность обоих крейтов сохранена (ре-экспорт), чтобы внешние
      импорты не сломались
- [+] Сверить наборы констант до и после переноса — перенос не должен менять значения

## Q15 — провайдер-сигнатура «AWS Secret (по контексту)»

- [+] Изучить по архиву legacy и BLUEPRINT §B.1, что именно требуется
- [+] Контекстное правило (у ключа нет отличительного префикса: 40 символов base64),
      обоснование в коде — почему именно такой контекст
- [+] Корпус: добавить записи под новый сигнал, литералы разбить через `concat!()`
      (push protection GitHub уже отклоняла синтетические фикстуры)
- [+] **I9: precision не падает ниже 1.000.** Если правило не держит precision — не
      сдавать его, а доложить: неаккуратная сигнатура хуже отсутствия
- [+] Не воскресить дубли находок на строку (правило приоритетов из прошлой задачи)

## Verification

- [+] `cargo fmt --all --check`
- [+] `cargo clippy --workspace --all-targets -- -D warnings`
- [+] `cargo test --workspace`
- [+] `cargo test -p codepack-engine --test golden` — 3/3
- [+] Корпус, реально перемерен: до — parity P=1.000 R=0.312 F1=0.476, full P=1.000
      R=1.000 F1=1.000; после — parity P=1.000 R=0.278 F1=0.435, full P=1.000 R=1.000
      F1=1.000. Precision не упала (I9). Падение parity-recall — следствие того, что в
      корпус добавлены два позитива, которых keyword-каскад не видит по устройству;
      в parity-режиме провайдер-сигнатур нет вовсе
- [+] `cargo xtask sync-agents --check`
- [-] `cargo deny check` — недоступен в песочнице (бинарь отсутствует), проверяется в CI
- [+] Независимое ревью диффа
- [+] CI на трёх ОС — теперь должен подняться от push в эту ветку

## Completion

- [+] `open-questions.md` — Q7, Q15, Q16 переведены в «закрыт»
- [+] `docs/architecture/overview.md` и `ROADMAP.md` обновлены
- [+] Чек-лист заполнен `+`/`-`
- [+] Финальный отчёт владельцу

## Next task

Этап **S10 — CLI / headless** (`ROADMAP.md` §3), там же Q6 (`.codepack.toml`).
