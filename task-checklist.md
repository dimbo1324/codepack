# Task Checklist

**Task:** Этап **S10 — CLI / headless** (`ROADMAP.md` §3) + открытый вопрос **Q6**
(проектный конфиг `.codepack.toml`, BLUEPRINT §B.7) — решением владельца он делается
здесь же.
**Date:** 2026-07-25
**Branch:** feat/s10-cli-headless

Первый этап транша «Интерфейс». Ядро (S0–S9) сдано и закалено; задача — дать ему точку
входа без графики, пригодную для CI/CD.

## Preparation

- [ ] Ритуал ориентации (git log, ROADMAP §1/§3, overview.md, предыдущий чек-лист,
      open-questions.md)
- [ ] Зафиксировать baseline: 751 тест, golden 3/3 — S10 не должен их сдвинуть
- [ ] Сверить, каких API движка не хватает для `preview`/`history`, и решить, что
      добавляется в ядро, а что живёт в CLI

## Реализация — структура крейта

- [ ] `main.rs` под 100 строк (правило для точек входа): только разбор аргументов,
      диспетчеризация и код возврата
- [ ] `cli.rs` —определения `clap`; `exit.rs` — коды возврата; `output.rs` — печать
      человеку и машине; `settings.rs` — разрешение конфигурации
- [ ] `commands/` — по модулю на команду

## Команды (ROADMAP §3)

- [ ] `export <path>` — полный пайплайн через `codepack_engine::run_export`
- [ ] `preview <path>` — только план, ничего не пишет в выходной каталог
- [ ] `scan <path>` — только сканер безопасности
- [ ] `history` — список прогонов из SQLite
- [ ] `doctor` — диагностика окружения, только чтение

## Флаги

- [ ] `--preset` (5 AI-пресетов), `--profile` (5 встроенных + пользовательские),
      `--safe-mode`, `--diff`, `--budget`, `--out`, `--json`
- [ ] `--budget` принимает `200k`/`1M`, а не только голое число (BLUEPRINT §B.5)
- [ ] Приоритет источников конфигурации задан явно и покрыт тестом:
      дефолты → глобальные настройки → `.codepack.toml` → флаги

## Коды возврата (контракт)

- [ ] `0` успех, `1` ошибка, `2` неверные аргументы, `3` найдены critical-секреты
- [ ] Код `3` не должен маскировать реальную ошибку; порядок приоритета кодов задан
      явно и покрыт тестом

## `--json` — машинный контракт

- [ ] Версионируется (`schema_version`), как того требует критерий готовности
- [ ] Схема каждой команды покрыта тестом на форму, а не только на «валидный JSON»
- [ ] Диагностика и прогресс идут в stderr, машинный вывод — только в stdout
      (иначе `--json` нельзя направить в конвейер)

## Q6 — `.codepack.toml`

- [ ] Формат, разбор, поиск от корня проекта
- [ ] Неизвестные ключи и битый файл: поведение выбрано осознанно и задокументировано
- [ ] Решение записать в `open-questions.md` (Q6 → закрыт)

## Verification

- [ ] `cargo fmt --all --check`
- [ ] `cargo clippy --workspace --all-targets -- -D warnings`
- [ ] `cargo test --workspace` — не ниже baseline 751
- [ ] `cargo test -p codepack-engine --test golden` — 3/3, ядро не задето
- [ ] Интеграционные тесты CLI: реальный запуск бинаря, а не только вызов функций
- [ ] Headless-экспорт в CI на трёх ОС (критерий готовности этапа)
- [ ] Независимое ревью диффа
- [ ] `cargo deny check` — ожидаемо недоступен в песочнице, проверяется в CI

## Completion

- [ ] `ROADMAP.md` — строка `**Status.**` для S10 + таблица §1
- [ ] `docs/architecture/overview.md` обновлён
- [ ] `open-questions.md` — Q6 закрыт
- [ ] Чек-лист заполнен `+`/`-`, финальный отчёт владельцу

## Next task

Этап **S11 — Tauri-оболочка и UI** (`ROADMAP.md` §3).
