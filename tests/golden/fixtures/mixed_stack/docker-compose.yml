version: "3.9"
services:
  web:
    build: ./frontend
    ports:
      - "3000:3000"
  api:
    build: ./backend
    environment:
      - JWT_SECRET=fixture-placeholder-value
    ports:
      - "8000:8000"
