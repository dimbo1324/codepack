{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The complete set of permissions the webview is granted. Deliberately minimal: no filesystem, no shell, no HTTP. Every file operation the UI needs goes through a #[tauri::command], and the dialog/opener plugins below hand the work to the OS rather than to the webview.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "opener:allow-open-path",
    "opener:allow-reveal-item-in-dir"
  ]
}
